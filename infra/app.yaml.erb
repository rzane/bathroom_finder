kind: Deployment
apiVersion: apps/v1beta1

metadata:
  name: app
  labels:
    app: app

spec:
  replicas: 2

  selector:
    matchLabels:
      app: app

  template:
    metadata:
      labels:
        app: app

    spec:
      containers:
      - name: app
        image: rzane/bathroom_finder:<%= current_sha %>
        ports:
          - containerPort: 4000
        livenessProbe:
          httpGet:
            path: /
            port: http
        readinessProbe:
          httpGet:
            path: /
            port: http
        env:
          - name: DATABASE_URL
            value: postgres://postgres:5432/bathroom_finder

---
apiVersion: v1
kind: Service

metadata:
  name: app
  labels:
    app: app

spec:
  type: LoadBalancer

  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP

  selector:
    app: app
